# Subagent: trajectory modeler (climb-delta predictor)
**Model:** Opus-class subagent · **Spawned by:** the main analysis session

## Instructions given
The thread owner's insight: the rank-signature model describes equilibrium (what Silver looks like); climbing is disequilibrium — what does a player whose rank is going UP look like, before it goes up? Build a lagged-panel predictor: sample players across Iron/Bronze/Silver, pull their recent ranked histories, split into adjacent windows, and regress forward window win rate on current window behavior (farm, vision, deaths, champion/role churn), with current rank and current-window WR as mean-reversion controls (matchmaking mechanically cools hot streaks), clustered by player. Riot has no rank-history endpoint, so forward WR is the trajectory outcome (LP is integrated W/L); probe op.gg lp_histories as a denser outcome for a subsample. Apply within-player to djfalcon's own 152-game panel: does his champion/role churn in one window predict his win-rate drop in the next — including whether behavior drift preceded his June Iron 1 → Iron 4 crash? Iterate publicly on the subtask issue; state all assumptions and limitations.
